package pse.nebula.worldview.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web configuration for web-related settings.
 * CORS is handled by the API Gateway, so no CORS configuration needed here.
 */
@Configuration
public class WebConfig {
    // CORS configuration removed - handled by API Gateway
}
