spring:
  application:
    name: gateway-service-test
  cloud:
    config:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: false

server:
  port: 0

eureka:
  client:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
