# =============================================================================
# Nebula Docker Environment Configuration - EXAMPLE TEMPLATE
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Main application database for storing routes, telemetry data, etc.
# The service uses 'world_view' schema within this database.
POSTGRES_DB=nebula_db
POSTGRES_USER=nebula_user
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
POSTGRES_PORT=5434

# -----------------------------------------------------------------------------
# RabbitMQ Message Broker Configuration
# -----------------------------------------------------------------------------
# RabbitMQ with MQTT plugin enabled for real-time coordinate streaming.
# Used by world-view service to publish journey updates to frontend.
RABBITMQ_USER=nebula
RABBITMQ_PASSWORD=password
RABBITMQ_VHOST=/

# RabbitMQ Port Mappings:
RABBITMQ_AMQP_PORT=5672
RABBITMQ_MQTT_PORT=1883
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_WEB_MQTT_PORT=15675

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis is used for token blacklist with automatic TTL-based expiration.
REDIS_PORT=6379
REDIS_PASSWORD=